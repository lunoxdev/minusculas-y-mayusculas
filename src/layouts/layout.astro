---
import "../styles/global.css";

const metadata = {
  title:
    "Convierta su texto a mayúsculas o minúsculas en cuestión de segundos con nuestra herramienta en línea.",
  description:
    "Convierte tu texto a mayúsculas o minúsculas en segundos con nuestro conversor en línea. Ahorra tiempo y esfuerzo con nuestra herramienta rápida, fácil de usar y eficiente.",
  keywords:
    "convertir, texto, mayúsculas, minúsculas, conversor en línea, rápido, fácil de usar, eficiente, herramienta en línea, simple, cambiar",
  url: "https://minusculasymayusculas.com/",
  urlimg: "https://minusculasymayusculas.com/logo.png",
};
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{metadata.title}</title>
    <meta name="description" content={metadata.description} />
    <meta name="keywords" content={metadata.keywords} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="canonical" href={metadata.url} />
    <meta property="og:title" content={metadata.title} />
    <meta property="og:description" content={metadata.description} />
    <meta property="og:image" content={metadata.urlimg} />
    <meta property="og:url" content={metadata.url} />
    <meta property="og:type" content="website" />
    <meta
      name="google-site-verification"
      content="EiVeXAlUPH8SO35GbfwodMcR7bAj7DAHoqYxJ2t1DcQ"
    />

    <!-- Facebook Meta Tags -->
    <meta property="og:url" content={metadata.url} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={metadata.description} />
    <meta property="og:description" content={metadata.description} />
    <meta property="og:image" content={metadata.urlimg} />

    <!-- Twitter Meta Tags -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta property="twitter:domain" content={metadata.url} />
    <meta property="twitter:url" content={metadata.url} />
    <meta name="twitter:title" content={metadata.description} />
    <meta name="twitter:description" content={metadata.description} />
    <meta name="twitter:image" content={metadata.urlimg} />
  </head>
  <body class="max-h-screen flex justify-center items-center mt-3 sm:mt-10 p-5">
    <main class="max-w-screen-sm space-y-6">
      <slot />
    </main>
  </body>
</html>
